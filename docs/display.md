# Display

qView can show compact file details in the unused background around the image. Enable
`Show details in image background` in the Window settings to draw a single-line HUD when the image
aspect ratio leaves enough grey margin.

The HUD shows the current position, filename, dimensions, and file size. It only draws in the
background margin and hides automatically when there is not enough room.
